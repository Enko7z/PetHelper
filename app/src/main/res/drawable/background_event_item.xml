<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Основной фон -->
    <solid android:color="@color/verylightbrown" />

    <!-- Скругленные углы -->
    <corners android:radius="8dp" />

    <!-- Граница -->
    <stroke android:width="1dp" android:color="@color/darkbrown" />

    <!-- Внутренние отступы -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
